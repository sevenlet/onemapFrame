import { defineComponent as r, ref as i, computed as g } from "vue";
const f = r({
  name: "PageSize",
  props: {
    // 每页显示条数
    pageSize: {
      type: Array,
      default: () => []
    },
    // 自定义样式
    sizeCustom: {
      type: String,
      default: ""
    }
  },
  emits: ["pagesize-change"],
  setup(a, p) {
    const t = i(10), u = i(`${t.value}条/页`), c = (e) => {
      t.value = e, p.emit("pagesize-change", e);
    }, o = g(() => {
      const e = [];
      let n = "", s = 0;
      return a.pageSize.length ? (a.pageSize.forEach((l) => {
        n = `${l}条/页`, s = l, e.push({ label: n, val: s });
      }), e) : null;
    });
    return {
      selectedPageNumTemp: u,
      selectedPageNum: t,
      selectedDisplay: o,
      valChange: c
    };
  }
});
export {
  f as default
};
