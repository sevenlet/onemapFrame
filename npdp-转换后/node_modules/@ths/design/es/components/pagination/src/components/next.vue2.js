import { defineComponent as a, ref as e, reactive as t } from "vue";
const s = a({
  name: "Next",
  props: {
    currentPage: {
      type: Number,
      default: 1
    },
    page: {
      type: Number || String,
      default: () => "-"
    },
    mini: {
      type: Boolean,
      default: !1
    },
    // 自定义下一页内容
    nextText: {
      type: String,
      default: "-"
    },
    // 总页码量
    pageSum: {
      type: Number || String,
      default: ""
    },
    // 自定义文字样式
    directionStyle: {
      type: String,
      default: ""
    }
  },
  setup(p) {
    const n = e("directionClass"), r = e("miniStyle"), i = t({
      fontSize: "12px",
      lineHeight: "24px"
    }), o = t({
      "background-color": "transparent",
      color: "#9fa4ae",
      cursor: "no-drop",
      opacity: "0.5"
    });
    return {
      // 基本变量
      miniClass: i,
      miniStyle: r,
      // 样式
      disabledStyle: o,
      directionClass: n
    };
  }
});
export {
  s as default
};
