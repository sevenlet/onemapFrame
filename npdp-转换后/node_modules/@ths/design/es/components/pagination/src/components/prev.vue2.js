import { defineComponent as a, ref as e, reactive as t } from "vue";
const l = a({
  name: "Pre",
  props: {
    currentPage: {
      type: Number,
      default: 1
    },
    mini: {
      type: Boolean,
      default: !1
    },
    // 自定义上一页内容
    prevText: {
      type: String,
      default: () => "-"
    },
    // 自定义文字样式
    directionStyle: {
      type: String,
      default: () => ""
    }
  },
  setup(p) {
    const n = e("directionClass"), o = t({
      fontSize: "12px",
      lineHeight: "24px"
    }), r = e("miniStyle"), i = t({
      "background-color": "transparent",
      cursor: "no-drop",
      color: "#9fa4ae",
      opacity: "0.5"
    });
    return {
      // 基本变量
      miniClass: o,
      miniStyle: r,
      // 样式
      disabledStyle: i,
      direction: n
    };
  }
});
export {
  l as default
};
