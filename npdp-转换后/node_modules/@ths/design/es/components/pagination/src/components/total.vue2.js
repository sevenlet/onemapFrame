import { defineComponent as t, ref as e } from "vue";
const n = t({
  name: "Total",
  props: {
    // 数据总量
    total: {
      type: Number,
      default: 0
    },
    // 数据总量展示模板
    showAllDataExample: {
      type: String,
      default: ""
    },
    // 标签样式
    tagClass: {
      type: String,
      default: () => ""
    },
    // mini模式
    mini: {
      type: Boolean,
      default: !1
    }
  },
  setup(a) {
    return {
      miniClass: e("miniClass")
    };
  }
});
export {
  n as default
};
