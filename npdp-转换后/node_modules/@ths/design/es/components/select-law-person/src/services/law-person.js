import s from "qs";
import i from "../utils/http.js";
const e = {
  // 获取当前登陆用户的机构信息
  getCurrentOrg: "/api/ouUserApi/getCurrentOrg",
  // 获取部门列表
  getDeptList: "/api/ouUserApi/listDept",
  // 获取单位列表
  getOrgList: "/api/ouUserApi/listOrg",
  // 获取人员列表
  getUserList: "/api/ouUserApi/listUser",
  // 查询-查询通用字典列表
  getDictList: "/dsm/dictionarycode/dictionary-code/listDictionary"
};
function n(t, r) {
  return i.get(t + e.getDictList, { params: r });
}
function p(t, r) {
  return i.get(t + e.getCurrentOrg, { params: r });
}
function u(t, r) {
  return i.post(t + e.getUserList, s.stringify(r));
}
export {
  e as IC_SELECT_LAW_PERSON_INTERFACE,
  p as getCurrentOrg,
  n as getDictList,
  u as getUserList
};
