import { defineComponent as e, computed as n } from "vue";
const i = e({
  name: "TSvgIcon",
  props: {
    // 图标颜色
    iconColor: {
      type: String,
      default: ""
    },
    // 图标路径
    iconSrc: {
      type: String,
      required: !0
    }
  },
  setup(o) {
    const r = n(() => o.iconColor), t = n(() => o.iconSrc);
    return {
      iColor: r,
      iSrc: t
    };
  }
});
export {
  i as default
};
