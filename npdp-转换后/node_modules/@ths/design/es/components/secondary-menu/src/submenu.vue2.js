import { inject as a, ref as u } from "vue";
const m = {
  name: "Submenu",
  props: {
    data: {
      type: Array,
      default: () => []
    },
    index: {
      type: Number,
      default: 1
    }
  },
  setup() {
    const e = a("rootMenu"), t = (s) => {
      e == null || e.rootMenuEmit("submenuItem:select", s);
    }, n = u(e == null ? void 0 : e.selectedMenus);
    return {
      rootMenu: e,
      activeMenu: n,
      handleSelect: t
    };
  }
};
export {
  m as default
};
