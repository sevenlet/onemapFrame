const $ = {
  /**
   * 已加载的资源缓存
   * @type {Set<string>}
   */
  loadedResources: /* @__PURE__ */ new Set(),
  /**
   * 并发加载处理：每个资源URL维护一个回调队列
   * @type {Object<string, Array<Function>>}
   */
  resourceCallbacks: {},
  /**
   * 加载JavaScript脚本
   * @param {string|Array<string>} urls - 单个URL或URL数组
   * @param {Function} [processFile] - 处理每个加载完成的文件的函数
   * @param {boolean} [appendToHead=true] - 是否添加到head（否则添加到body）
   * @returns {Promise} - 所有文件加载完成的Promise
   */
  loadScripts(n, o, s = !0) {
    const e = Array.isArray(n) ? n : [n];
    return new Promise((i, l) => {
      if (!e || e.length === 0) {
        i([]);
        return;
      }
      let c = 0;
      const r = e.length;
      e.forEach((t) => {
        if (this.loadedResources.has(t)) {
          o && o(t), c++, c === r && i([]);
          return;
        }
        if (this.resourceCallbacks[t]) {
          this.resourceCallbacks[t].push(() => {
            o && o(t), c++, c === r && i([]);
          });
          return;
        }
        this.resourceCallbacks[t] = [];
        const a = document.createElement("script");
        a.type = "text/javascript", a.src = t, a.onload = () => {
          this.loadedResources.add(t), o && o(t), this.resourceCallbacks[t].forEach((d) => d && d()), delete this.resourceCallbacks[t], c++, c === r && i([]);
        }, a.onerror = (d) => {
          console.error(`加载脚本出错: ${t}`, d), this.resourceCallbacks[t].forEach((u) => u && u()), delete this.resourceCallbacks[t], c++, c === r && i([]);
        }, s ? document.head.appendChild(a) : document.body.appendChild(a);
      });
    });
  },
  /**
   * 加载CSS样式表
   * @param {string|Array<string>} urls - 单个URL或URL数组
   * @returns {Promise} - 所有样式表加载完成的Promise
   */
  loadStyles(n) {
    const o = Array.isArray(n) ? n : [n];
    return new Promise((s) => {
      if (!o || o.length === 0) {
        s([]);
        return;
      }
      let e = 0;
      const i = o.length;
      o.forEach((l) => {
        const c = document.querySelectorAll('link[rel="stylesheet"]');
        if (Array.from(c).some((a) => {
          var d;
          return ((d = a == null ? void 0 : a.href) == null ? void 0 : d.indexOf(l)) > -1;
        }) || this.loadedResources.has(l)) {
          e++, e === i && s([]);
          return;
        }
        const t = document.createElement("link");
        t.rel = "stylesheet", t.type = "text/css", t.href = l, t.onload = () => {
          this.loadedResources.add(l), e++, e === i && s([]);
        }, t.onerror = () => {
          console.error(`加载样式表出错: ${l}`), e++, e === i && s([]);
        }, document.head.appendChild(t);
      });
    });
  }
}, w = {}, x = {
  async loadComponent(n, o) {
    var l, c;
    if (!n || typeof n != "string") {
      o && o();
      return;
    }
    const s = `${n}Component`, e = window, i = (r) => {
      if (!r) {
        o && o();
        return;
      }
      e[s] = r, e.app && typeof e.app.component == "function" && (e.app.component(s) || e.app.component(s, r)), e.router && typeof e.router.hasRoute == "function" && !e.router.hasRoute(s) && e.router.addRoute({
        path: `/${n}`,
        name: s,
        component: r
      }), e.internalKeyRef && e.internalKeyRef.value++, o && o();
    };
    if (e[s]) {
      i(e[s]);
      return;
    }
    try {
      let r = w[n], t;
      if (!r && ((l = e.ComponentLoaderInternal) != null && l.componentModules)) {
        const h = e.ComponentLoaderInternal.componentModules;
        t = Object.keys(h).find(
          (f) => f.endsWith(`/${n}.vue`) || f.endsWith(`/${n}/index.vue`) || f.includes(`/${n}/index.js`)
        ), t && (r = h[t], w[n] = r);
      }
      if (r) {
        const h = await r();
        if (t && t.endsWith("/index.js")) {
          const f = t.replace("/index.js", "/index.css"), p = (c = e.ComponentLoaderInternal) == null ? void 0 : c.cssModules;
          if (p && p[f])
            try {
              const m = await p[f]();
              if (m && typeof m == "string") {
                const y = document.createElement("style");
                y.textContent = m, document.head.appendChild(y);
              }
            } catch (m) {
              console.error(`ComponentLoader: CSS 加载失败 [${f}]`, m);
            }
        }
        i(h.default || h);
        return;
      }
      const a = typeof e.ThsDesignUmdResolver == "function" ? e.ThsDesignUmdResolver : (h) => {
        const f = (e.ThsDesignUmdBase || "src/").replace(/\/?$/, "/");
        return {
          js: `${f}${h}/index.js`,
          css: `${f}${h}/index.css`
        };
      }, { js: d, css: u } = a(n) || {};
      if (u && await $.loadStyles(Array.isArray(u) ? u : [u]).catch(() => {
      }), d) {
        if (await $.loadScripts(Array.isArray(d) ? d : [d], void 0), e[s]) {
          i(e[s]);
          return;
        }
        console.error(
          `ComponentLoader: 加载 [${d}] 完成，但 window.${s} 仍未定义。 请确认 UMD 包导出格式：(function(global){ global.${s} = ...; })(window)`
        );
      }
      o && o();
    } catch (r) {
      console.error(`动态加载组件失败 [${n}]:`, r), o && o();
    }
  }
};
export {
  x as ComponentLoader,
  $ as ResourceLoader
};
