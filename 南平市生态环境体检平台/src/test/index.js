window.testComponent = {
template: 
`<div id="t-l-c-test" class="t-l-c-test" >` +
`<t-iframe id="t-iframe-8385.ed81cfb2c" class="test-t-iframe-0-0"  :src="tIframex0x0ComputedData.src" :srcdoc="tIframex0x0ComputedData.srcdoc" :scrolling="tIframex0x0ComputedData.scrolling" :c-style="tIframex0x0ComputedData.cStyle" :socket-url="replaceCssVariables(filterData(global.socketIp, componentPropBindingMap?.['t-iframe-8385.ed81cfb2c']['socketUrl'].filters || []), global?.theme || global?.activeTheme || '默认主题')" :room-id="replaceCssVariables(filterData(global.socketRoom, componentPropBindingMap?.['t-iframe-8385.ed81cfb2c']['roomId'].filters || []), global?.theme || global?.activeTheme || '默认主题')">` +
`</t-iframe>` +
`</div>` +
'',
setup() {
const componentPropBindingMap = {"body":{"attributeName":"tBodyx0x0"},"t-iframe-8385.ed81cfb2c":{"attributeName":"tIframex0x0","src":{"filters":[]},"srcdoc":{"filters":[]},"scrolling":{"filters":[]},"cStyle":{"filters":[]},"socketUrl":{"filters":[]},"roomId":{"filters":[]}}};
// 页面内注入全局变量和事件
const rootData = inject('root');
const global = reactive({
...toRefs(rootData)
});
// 路由
const route = useRoute();
// 创建 DOM 引用

// 当前页面使用的变量
const state = Vue.reactive({
   screenSize: "",
});
// 选中的图表主题
const activeChartTheme = computed(() => {
const activeTheme = _.cloneDeep(themes.find((item) => item.name === global.activeTheme));
return activeTheme.chartVariables;
});
// 选中的图表主题的options
const activeChartThemeOptions = computed(() => {
if (activeChartTheme.value) {
// 公用的主题配置
const themeConfig = {
backgroundColor: activeChartTheme.value.backgroundColor, // 背景色
color: activeChartTheme.value.color, // 系列颜色
title: activeChartTheme.value.title, // 标题
legend: activeChartTheme.value.legend, // 图例
tooltip: activeChartTheme.value.tooltip, // 提示框
xAxis: activeChartTheme.value.categoryAxis, // x轴
yAxis: activeChartTheme.value.valueAxis, // y轴
};
return themeConfig;
} else {
return {};
}
});
// 当前页面中组件的属性
const componentState = Vue.reactive({
tIframex0x0: {
defaultStyle: {
            default: {"width":"1921px","height":"1074px","position":"unset","left":"-4px","top":"4px"},
            
            },
 default: { 
 
src:"",
srcdoc:"<!--\n * @Author: lvzhixin\n * @Date: 2024-05-30 13:33:18\n * @LastEditTime: 2025-11-07 15:37:34\n * @LastEditors: 郭鹏铭\n * @Description: File Info\n * @Description: 复选框显示逻辑：必须是叶子节点：!node.isLeaf 为 false，即只有叶子节点（没有子节点的节点）才会显示复选框\n * @Description: 如果 EXT1 字段中的 type 为 'layer'，则显示复选框，对于普通专题，根据 isShowSubjectCheckbox 状态决定\n-->\n<!DOCTYPE html>\n<html lang=\"en\">\n\n<head>\n    <meta charset=\"UTF-8\" />\n    <title>我的应用名称</title>\n    <link href=\"assets/images/favicon.ico\" rel=\"shortcut icon\" />\n    <!-- vue 这里最终应该下载一个稳定版本 -->\n    <script src=\"./resources/vue.min.js\"></script>\n\n    <!-- socket插件 -->\n    <script src=\"./resources/socket.io.js\"></script>\n\n    <!-- lodash插件 -->\n    <script src=\"./resources/lodash.min.js\"></script>\n\n    <!-- 引入样式 -->\n    <link href=\"./resources/element-plus/element-plus.min.css\" rel=\"stylesheet\" />\n    <!-- 引入组件库 -->\n    <script src=\"./resources/element-plus/element-plus.min.js\"></script>\n    <script src=\"./resources/element-plus/zhCn.js\"></script>\n\n    <!-- 通信插件mitt https://www.npmjs.com/package/mitt -->\n    <script src=\"./resources/mitt.min.js\"></script>\n\n    <!-- HTTP库 -->\n    <script src=\"./resources/axios.min.js\"></script>\n\n    <!-- 主题样式 -->\n    <link href=\"./assets/style/themes.css\" rel=\"stylesheet\" />\n\n    <style>\n        body {\n            margin: 0;\n        }\n\n        body,\n        html,\n        #app {\n            height: 100%;\n        }\n\n        .subject {\n            height: 100%;\n            /* width: 193px; */\n            width: 100%;\n            /* background: rgba(6, 18, 8, 0.4); */\n        }\n\n        .subject .el-scrollbar__view {\n            padding-left: 8px;\n            padding-right: 8px;\n        }\n\n        .subject .menu-tree {\n            margin-bottom: 20px;\n        }\n\n        .subject .visible-tree {\n            /* 显示时的样式 */\n            display: block;\n        }\n\n        .subject .hidden-tree {\n            /* 隐藏时的样式，使用绝对定位脱离文档流 */\n            position: absolute;\n            top: -9999px;\n            left: -9999px;\n            pointer-events: none;\n        }\n\n        /* 复选框样式 */\n        .subject .el-checkbox {\n            --el-checkbox-background-color: var(--left-menu-checkbox-bg);\n            --el-border-base: 1px solid var(--left-menu-checkbox-border);\n            --el-checkbox-checked-background-color: #3b382a;\n            --el-checkbox-checked-input-border-color: #fff198;\n            --el-checkbox-input-border-color-hover: #fff198;\n            margin-right: 6px;\n            vertical-align: middle;\n        }\n\n        .subject .el-checkbox__input.is-focus:not(.is-checked) .el-checkbox__inner:not(:hover) {\n            border-color: var(--left-menu-checkbox-border);\n        }\n\n        /* 树样式 */\n        .subject .el-tree {\n            --el-tree-node-hover-background-color: transparent;\n            --el-tree-font-color: #d8ffe9;\n            --el-tree-expand-icon-color: #fff;\n\n            background: transparent;\n        }\n\n        /* .subject .el-tree + .el-tree{\n        margin-top: 20px;\n      } */\n\n        /* 树-箭头样式-非叶子节点 */\n        .subject .el-icon-caret-right:not(.is-leaf):before {\n            border-radius: 3px;\n            background: var(--left-menu-tree-unpack-bg);\n        }\n\n        /* 树-箭头样式 */\n        .subject .el-icon-caret-right:before {\n            padding: 3px;\n            font-size: 9px;\n        }\n\n        /* 每一个一级菜单容器 */\n        .subject .el-tree>.el-tree-node {\n            border-radius: 18px;\n            box-sizing: border-box;\n            /* background: left 0 bottom -3px / contain var(--left-menu-bottom-line-1) no-repeat,\n          right 0 bottom -3px / contain var(--left-menu-bottom-line-1) no-repeat,\n          18px bottom / calc(100% - 36px) auto var(--left-menu-bottom-line) no-repeat,\n          left bottom / 18px auto var(--left-menu-bottom-left-corner) no-repeat,\n          right bottom / 18px auto var(--left-menu-bottom-right-corner) no-repeat;\n        background-color: rgba(1, 32, 15, 0.5); */\n            position: relative;\n        }\n\n        .subject .el-tree>.el-tree-node::before {\n            content: \" \";\n            display: block;\n            position: absolute;\n            left: 0;\n            top: 0;\n            z-index: -1;\n            width: 100%;\n            height: 100%;\n            border-style: solid;\n            border-image-outset: 0;\n            border-image-repeat: stretch;\n            border-width: 15px 15px;\n            border-image-source: var(--left-menu-tree-bg);\n            border-image-slice: 15 15 fill;\n            border-image-width: 1;\n            box-sizing: border-box;\n        }\n\n        /* 相邻的一级菜单容器 */\n        .subject .el-tree>.el-tree-node+.el-tree-node {\n            margin-top: 20px;\n        }\n\n        .subject .el-tree .el-tree-node .el-tree-node__content {\n            padding-right: 6px;\n        }\n\n        /* 所有的菜单树节点容器 */\n        .subject .el-tree-node {\n            padding-left: 22px;\n        }\n\n        /* 第一个及第一个菜单项的第一个子菜单项的菜单树节点容器 */\n        .subject .el-tree>.el-tree-node,\n        .subject .el-tree>.el-tree-node>.el-tree-node__children>.el-tree-node {\n            padding-left: 0;\n        }\n\n        /* 二级菜单项叶子节点 */\n        .subject .el-tree>.el-tree-node>.el-tree-node__children>.el-tree-node>.el-tree-node__content .el-tree-node__expand-icon.is-leaf {\n            display: block;\n            margin-left: 0;\n        }\n\n        /* 菜单项叶子节点 */\n        .el-tree-node__children .el-tree-node__content>.el-tree-node__expand-icon.is-leaf {\n            display: none;\n            padding-left: 0px;\n        }\n\n        /* 菜单项叶子节点相邻的复选框 */\n        .el-tree-node__children .el-tree-node__content>.el-tree-node__expand-icon.is-leaf+.el-checkbox {\n            margin-left: 6px;\n        }\n\n        /* 一级菜单的默认样式 */\n        .subject .el-tree>.el-tree-node>.el-tree-node__content {\n            display: flex;\n            flex-direction: row-reverse;\n            justify-content: space-between;\n            padding: 6px 0 6px 14px !important;\n            /* border-radius: 18px;\n        background: radial-gradient(\n            52% 52% at 50% 113%,\n            rgba(123, 255, 182, 0.6) 0%,\n            rgba(11, 143, 70, 0) 100%\n          ),\n          rgba(6, 72, 35, 0.5); */\n            box-sizing: border-box;\n            /* border: 1px solid;\n        border-image: radial-gradient(\n            39% 39% at 47% 101%,\n            #8affbd 0%,\n            rgba(0, 255, 111, 0) 100%\n          )\n          1;\n        box-shadow: inset 5px 0px 6px 0px rgba(138, 255, 189, 0.302); */\n            background: var(--left-menu-tree-title-bg);\n            background-repeat: no-repeat;\n            background-size: 100% 100%;\n        }\n\n        /* 一级菜单的高亮样式 */\n        .subject .el-tree.el-tree--highlight-current>.el-tree-node.is-current>.el-tree-node__content {\n            padding: 6px 16px;\n            margin-bottom: 3px;\n            background: #252f26;\n            box-sizing: border-box;\n            border: 1px solid #ffefc9;\n            box-shadow: inset 0px 0px 18px 0px rgba(255, 206, 109, 0.49);\n            border-radius: 18px;\n            font-weight: normal;\n            color: #ffec9f;\n        }\n\n        .subject .el-tree>.el-tree-node.is-expanded>.el-tree-node__children {\n            padding-bottom: 4px;\n        }\n\n        /* 树节点 */\n        .subject .el-tree-node__content {\n            height: 36px;\n            /* padding-left: 0 !important; */\n        }\n\n        /* 隐藏一级菜单的复选框 */\n        /* .subject .el-scrollbar__view + .el-tree > .el-tree-node > .el-tree-node__content .el-checkbox {\n        display: none;\n      } */\n\n        /* 所有菜单项的复选框 */\n        .subject .el-tree-node__content>label.el-checkbox {\n            margin-right: 6px;\n        }\n\n        /* 树-箭头样式-非叶子节点 */\n        .subject .el-tree-node__content .el-tree-node__expand-icon:not(.is-leaf)+.el-checkbox {\n            display: none;\n        }\n\n        /* 调整一级菜单的下拉箭头伪元素样式 */\n        .subject .el-tree>.el-tree-node>.el-tree-node__content .el-tree-node__expand-icon::before {\n            content: \"\";\n            display: block;\n            width: 24px;\n            height: 24px;\n            /* background: transparent; */\n            background: var(--left-menu-subject-dropdown-arrow) 28px -7px / cover;\n            /* background: url('http://192.168.3.107:8088/assets/images/menu-list/arrow-green.svg') 28px -7px / cover; */\n        }\n\n        /* 一级菜单箭头的高亮样式 */\n        .subject .el-tree>.el-tree-node.is-current>.el-tree-node__content .el-tree-node__expand-icon::before {\n            background: var(--left-menu-subject-dropdown-arrow-active) 28px -7px / cover;\n        }\n\n        /* 调整一级菜单的下拉箭头-展开样式 */\n        .subject .el-tree>.el-tree-node>.el-tree-node__content>.el-tree-node__expand-icon.expanded {\n            transform: rotate(0deg);\n        }\n\n        /* 调整一级菜单的下拉箭头-默认样式 */\n        .subject .el-tree>.el-tree-node>.el-tree-node__content>.el-tree-node__expand-icon {\n            transform: rotate(-90deg);\n        }\n\n        /* 菜单（非一级）的高亮样式 */\n        .subject .el-tree--highlight-current .el-tree-node.is-current>.el-tree-node__content {\n            background-color: transparent;\n        }\n\n        /* 超出内容宽度的省略号样式 */\n        .subject .custom-tree-node {\n            overflow: hidden;\n            text-overflow: ellipsis;\n        }\n\n        /* 菜单文本 */\n        .subject .folder-label {\n            font-size: 14px;\n            line-height: 20px;\n        }\n\n        /* 菜单文本 */\n        .subject .folder-label.active {\n            color: #ffec9f;\n            text-shadow: 0px 0px 10px rgba(255, 235, 187, 0.5);\n            font-weight: bold;\n        }\n\n        /* 菜单文本-禁用状态 */\n        .subject .el-tree--highlight-current .el-tree-node.is-current>.el-tree-node__content .folder-label.disabled {\n            cursor: not-allowed;\n        }\n\n        .subject .el-scrollbar {\n            --el-scrollbar-opacity: 1;\n            --el-scrollbar-background-color: var(--left-menu-scrollbar-bg);\n            --el-scrollbar-hover-opacity: 1;\n            --el-scrollbar-hover-background-color: var(--left-menu-scrollbar-bg);\n        }\n\n        /* 筛选面板样式 */\n        .filter-panel {\n            position: relative;\n            width: 100%;\n            background: rgba(0, 30, 60, 0.9);\n            border: 1px solid #1e88e5;\n            border-radius: 4px;\n            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);\n            margin-top: 10px;\n            margin-bottom: 10px;\n            padding: 15px;\n            box-sizing: border-box;\n            color: #ffffff;\n        }\n\n        .panel-header {\n            display: flex;\n            justify-content: space-between;\n            align-items: center;\n            margin-bottom: 15px;\n            padding-bottom: 8px;\n            border-bottom: 1px solid #1e88e5;\n        }\n\n        .panel-title {\n            font-size: 16px;\n            font-weight: bold;\n            color: #90caf9;\n        }\n\n        .search-box {\n            margin-bottom: 15px;\n        }\n\n        .search-input {\n            width: 100%;\n            padding: 8px 12px;\n            background: rgba(255, 255, 255, 0.1);\n            border: 1px solid #1e88e5;\n            border-radius: 4px;\n            color: #ffffff;\n            font-size: 14px;\n            box-sizing: border-box;\n        }\n\n        .search-input::placeholder {\n            color: rgba(255, 255, 255, 0.5);\n        }\n\n        .search-input:focus {\n            outline: none;\n            border-color: #64b5f6;\n            box-shadow: 0 0 0 2px rgba(100, 181, 246, 0.2);\n        }\n\n        .tag-list {\n            display: flex;\n            flex-wrap: wrap;\n            gap: 8px;\n        }\n\n        .tag {\n            padding: 4px 12px;\n            background: rgba(255, 255, 255, 0.1);\n            border: 1px solid #1e88e5;\n            border-radius: 16px;\n            font-size: 12px;\n            color: #e3f2fd;\n            cursor: pointer;\n            transition: all 0.3s;\n        }\n\n        .tag:hover {\n            background: rgba(30, 136, 229, 0.2);\n            border-color: #64b5f6;\n        }\n\n        .tag.active {\n            background: #1e88e5;\n            border-color: #64b5f6;\n            color: #ffffff;\n        }\n\n        /* 蓝色主题变量 */\n        body.蓝色版 {\n            --theme-color: #1e88e5;\n            --theme-font-color: #e3f2fd;\n            --theme-background-color: rgba(0, 30, 60, 0.9);\n            --theme-selector-background-color: rgba(30, 136, 229, 0.2);\n            --theme-border-color: #1e88e5;\n            --theme-hover-color: #64b5f6;\n        }\n    </style>\n</head>\n\n<body class=\"蓝色版\">\n    <div id=\"app\"></div>\n\n    <script>\n        /**\n         * 这里定义全局使用的变量、方法\n         */\n        const {\n            createApp,\n            computed,\n            ref,\n            reactive,\n            toRefs,\n            onBeforeMount,\n            onMounted,\n            onBeforeUpdate,\n            onUpdated,\n            onBeforeUnmount,\n            onUnmounted,\n            onErrorCaptured,\n            onRenderTracked,\n            onRenderTriggered,\n            watch,\n            provide,\n            inject,\n            nextTick\n        } = Vue;\n\n        // 房间号\n        const roomId = localStorage.getItem('lxtSocketRoom');\n\n        // 主题\n        const themeName = localStorage.getItem('lxtTheme');\n\n        const bodyDom = document.querySelector('body');\n        if (bodyDom && themeName) {\n            bodyDom.className = themeName;\n        }\n\n        /**\n         * 这里定义根组件内容\n         */\n        const subjectComponent = {\n            template: `\n      <div class=\"subject\">\n        <el-scrollbar max-height=\"100%\">\n          <el-tree\n              ref=\"menuTreeRef\"\n              class=\"menu-tree\"\n              :class=\"{ 'visible-tree': showMenuTree, 'hidden-tree': !showMenuTree }\"\n              default-expand-all\n              highlight-current\n              node-key=\"RES_ID\"\n              empty-text=\"无数据\"\n              :props=\"propsMap\"\n              :data=\"menuData\"\n              :indent=\"0\"\n              :current-node-key=\"currentMenuItem?.RES_ID\"\n              @node-click=\"handleClickNode\"\n          >\n            <template #default=\"{ node, data }\">\n                  <span class=\"custom-tree-node\">\n                    <el-checkbox\n                        v-if=\"checkboxVisible(node, data)\"\n                        v-model=\"node.checked\"\n                        @click.stop=\"\"\n                        @change=\"handleCheck(data, node)\"\n                    >\n                    </el-checkbox>\n                    <span\n                        class=\"folder-label\"\n                        :class=\"{ active: currentMenuItem.RES_ID === data.RES_ID && node.isLeaf }\"\n                        :title=\"data[propsMap.label]\"\n                    >\n                      <!-- 节点文本 -->\n                      {{ node.label }}\n                    </span>\n                  </span>\n            </template>\n          </el-tree>\n          <!-- 专题图层 -->\n          <el-tree\n              ref=\"layerMenuTreeRef\"\n              default-expand-all\n              highlight-current\n              node-key=\"RES_ID\"\n              check-on-click-node\n              empty-text=\" \"\n              :props=\"propsMap\"\n              :data=\"layerMenuData\"\n              :indent=\"0\"\n              :current-node-key=\"currentLayerMenuItem?.RES_ID\"\n              :check-strictly=\"true\"\n              :key=\"layerMenuTreeKey\"\n              @node-click=\"handleLayerMenuCheck\"\n          >\n            <template #default=\"{ node, data }\">\n                  <span class=\"custom-tree-node\">\n                    <el-checkbox\n                        v-if=\"checkboxVisible(node, data)\"\n                        v-model=\"node.checked\"\n                        @click.stop=\"\"\n                        @change=\"handleLayerMenuCheck(data, node)\"\n                    >\n                    </el-checkbox>\n                    <span\n                        class=\"folder-label\"\n                        :class=\"{ active: currentLayerMenuItem.RES_ID === data.RES_ID && node.isLeaf }\"\n                        :title=\"data[propsMap.label]\"\n                    >\n                      <!-- 节点文本 -->\n                      {{ node.label }}\n                    </span>\n                  </span>\n            </template>\n          </el-tree>\n          \n          <!-- 筛选面板组件 -->\n          <div class=\"filter-panel\">\n              <div class=\"panel-header\">\n                  <span class=\"panel-title\">筛选条件</span>\n              </div>\n              <div class=\"panel-content\">\n                  <div class=\"search-box\">\n                      <el-input\n                          v-model=\"searchKeyword\"\n                          placeholder=\"请输入关键词搜索\"\n                          clearable\n                          @input=\"handleSearch\"\n                          class=\"search-input\"\n                      />\n                  </div>\n                  <div class=\"tag-section\">\n                      <div class=\"section-title\">标签筛选</div>\n                      <div class=\"tag-list\">\n                          <span \n                              v-for=\"tag in tagList\" \n                              :key=\"tag\"\n                              class=\"tag\"\n                              :class=\"{ active: selectedTags.includes(tag) }\"\n                              @click=\"clickTag(tag)\"\n                          >\n                              {{ tag }}\n                          </span>\n                      </div>\n                  </div>\n              </div>\n          </div>\n        </el-scrollbar>\n      </div>\n    `,\n            setup() {\n                // 页面内注入全局变量和事件\n                const rootData = inject('root');\n\n                const global = reactive({\n                    ...toRefs(rootData)\n                });\n\n                // 配置选项的映射\n                const propsMap = {\n                    children: 'CHILDREN',\n                    label: 'RES_NAME'\n                };\n\n                // 树形菜单组件\n                const menuTreeRef = ref(null);\n                const layerMenuTreeRef = ref(null);\n\n                // 格式化菜单的一些操作\n                // const formatMenuTreeOperate = useFormatMenuTree();\n                // console.log(localStorage.getItem('isShowSubjectCheckbox') ? localStorage.getItem('isShowSubjectCheckbox') === 'true' : false, 9999)\n\n                const state = reactive({\n                    // 是否展示专题的复选框\n                    isShowSubjectCheckbox: localStorage.getItem('isShowSubjectCheckbox') ? localStorage.getItem('isShowSubjectCheckbox') === 'true' : false,\n                    // 树形菜单数据\n                    data: [],\n                    // 专题数据\n                    menuData: [],\n                    // 是否显示专题菜单\n                    showMenuTree: true,\n                    // 图层专题数据\n                    layerMenuData: [],\n                    // 当前选中的菜单项（专题使用）\n                    currentMenuItem: {},\n                    // 当前选中的菜单项（图层专题使用）\n                    currentLayerMenuItem: {},\n                    // 当前勾选上的菜单项的id列表\n                    checkedIds: [],\n                    // 当前勾选上的菜单项数据\n                    checkedData: [],\n                    // 当前高亮的一级菜单项id（专题使用）\n                    currentActiveFirstLevelMenuId: null,\n                    // 当前高亮的一级菜单项id（图层专题使用）\n                    currentActiveFirstLevelLayerMenuId: null,\n                    // 右侧面板数据\n                    rightPanelData: [],\n                    // 图层专题的索引，处理树不刷新的问题使用的\n                    layerMenuTreeKey: 0,\n                    // 筛选面板相关状态\n                    searchKeyword: '',\n                    tagList: ['全部', '标签1', '标签2', '标签3', '标签4', '标签5'],\n                    selectedTags: ['全部']\n                });\n\n                /**\n                 * 判断树形数据是否每一级只有一个节点\n                 * @param {Array} treeData - 树形结构数据\n                 */\n                function checkShowMenuTree() {\n                    const treeData = state.menuData;\n                    // 有数据就显示菜单树，无论每层节点数量多少\n                    return Array.isArray(treeData) && treeData.length > 0;\n                }\n\n                /**\n                 * 设置一级节点高亮\n                 * @param node - 节点\n                 */\n                function setFirstNodeActive(node, type) {\n                    let parentNode = node.parent;\n                    if (parentNode && node.level !== 1) {\n                        setFirstNodeActive(parentNode, type);\n                    } else {\n                        if (Array.isArray(parentNode.childNodes)) {\n                            parentNode.childNodes.forEach((item) => {\n                                item.isCurrent = false; // 取消所有节点的选中状态 防止选中多个节点\n                            });\n                        }\n                        // 当前节点的一级父节点选中\n                        node.isCurrent = true;\n                        if (type && type === 'layer') {\n                            // 记录当前高亮的一级菜单项id（图层专题使用）\n                            state.currentActiveFirstLevelLayerMenuId = node.data.RES_ID;\n                        } else {\n                            // 记录当前高亮的一级菜单项id（专题使用）\n                            state.currentActiveFirstLevelMenuId = node.data.RES_ID;\n                        }\n                    }\n                }\n\n                /**\n                 * 发送setAppAR或者setLayersAR消息，地图去添加/删除专题图层\n                 * @param data - 专题数据\n                 * @param opType - 操作类型，add或remove\n                 * @param highlight - 当前图层是否高亮\n                 */\n                function sendSetAppARMsg(data, opType) {\n                    console.log(data, \"当前点击的数据12\");\n                    try {\n                        // setAppAR消息的数据\n                        let setAppARMsgData = [];\n                        // setLayersAR消息的数据\n                        let setLayersARMsgData = [];\n\n                        if (data?.length) {\n                            data.forEach((item) => {\n                                // 目录的扩展字段\n                                const extendField = item.EXT1 ? JSON.parse(item.EXT1) : {};\n                                // console.log(extendField, \"extendField\");\n                                // type 字段不存在则代表是专题\n                                if (!extendField.type) {\n                                    setAppARMsgData.push({\n                                        resCode: extendField?.resCode || '',\n                                        opType,\n                                        resName: item.RES_NAME,\n                                        appURL: extendField?.appURL || '',\n                                        highlight: item.RES_ID === state.currentMenuItem.RES_ID\n                                    });\n                                } else if (extendField.type && extendField.type === 'layer') {\n                                    //type 字段存在并且等于layer则代表是图层\n                                    setLayersARMsgData.push({\n                                        libraryId: extendField.libraryId || extendField.resCode,\n                                        layerId: extendField.resCode,\n                                        opType\n                                    });\n                                }\n                            });\n                        } else if (!Array.isArray(data) && data.EXT1) {\n                            // 目录的扩展字段\n                            const extendField = data.EXT1 ? JSON.parse(data.EXT1) : {};\n\n                            // type 字段不存在则代表是专题\n                            if (!extendField.type) {\n                                // setLayersAR消息的数据\n                                setAppARMsgData = [\n                                    {\n                                        resCode: extendField?.resCode || '',\n                                        opType,\n                                        resName: data.RES_NAME,\n                                        appURL: extendField?.appURL || '',\n                                        highlight: data.RES_ID === state.currentMenuItem.RES_ID\n                                    }\n                                ];\n                            } else if (extendField.type && extendField.type === 'layer') {\n                                //type 字段存在并且等于layer则代表是图层\n                                setLayersARMsgData = [\n                                    {\n                                        libraryId: extendField.libraryId || extendField.resCode,\n                                        layerId: extendField.resCode,\n                                        opType\n                                    }\n                                ];\n                            }\n                        }\n\n                        if (setAppARMsgData.length) {\n                            // 发送socket消息: 地图添加/删除专题\n                            rootData.rootSocket.emit('message', {\n                                room: global.socketRoom, // 房间号\n                                type: 'setAppAR', // 消息名称\n                                data: setAppARMsgData\n                            });\n                            console.log(\n                                {\n                                    room: global.socketRoom, // 房间号\n                                    type: 'setAppAR', // 消息名称\n                                    data: setAppARMsgData\n                                },\n                                `地图${opType}专题`\n                            );\n                        }\n\n                        if (setLayersARMsgData.length) {\n                            // 发送socket消息: 地图添加/删除专题图层\n                            rootData.rootSocket.emit('message', {\n                                room: global.socketRoom, // 房间号\n                                type: 'setLayersAR', // 消息名称\n                                data: setLayersARMsgData\n                            });\n                            console.log(\n                                {\n                                    room: global.socketRoom, // 房间号\n                                    type: 'setLayersAR', // 消息名称\n                                    data: setLayersARMsgData\n                                },\n                                `地图${opType}专题图层`\n                            );\n                        }\n                    } catch (error) {\n                        console.log(error, 'EXT1扩展字段数据格式不对');\n                    }\n                }\n\n                /**\n                 * 发送showRightView消息\n                 * @param data - 专题数据\n                 * @param operation - 操作类型，add | remove | highlight\n                 * @param menuType - 类型，专题还是专题图层\n                 */\n                function sendShowRightViewMsg(data, operation, menuType) {\n                    // 消息数据\n                    let msgData = [data];\n\n                    /* 若是目录 */\n                    if (data[propsMap.children]?.length) {\n                        if (operation === 'add') {\n                            // 过滤掉已勾选的图层\n                            msgData = data[propsMap.children].filter(\n                                (item) =>\n                                    !state.checkedIds.includes(item.RES_ID) &&\n                                    state.currentMenuItem.RES_ID !== item.RES_ID\n                            );\n                        } else if (operation === 'remove') {\n                            // 过滤掉菜单高亮图层\n                            msgData = data[propsMap.children].filter(\n                                (item) => state.currentMenuItem.RES_ID !== item.RES_ID\n                            );\n                        }\n                    }\n\n                    // 发送socket消息: 地图删除专题\n                    rootData.rootSocket.emit('message', {\n                        room: global.socketRoom, // 房间号\n                        type: 'showRightView', // 消息名称\n                        // 发送的数据\n                        data: {\n                            data: msgData, // 操作的菜单数据\n                            operation, // 操作类型\n                            type: menuType || 'subject' // 专题\n                        }\n                    });\n                    console.log(\n                        {\n                            room: global.socketRoom, // 房间号\n                            type: 'showRightView', // 消息名称\n                            // 发送的数据\n                            data: {\n                                data: msgData, // 操作的菜单数据\n                                operation, // 操作类型\n                                type: menuType || 'subject' // 专题\n                            }\n                        },\n                        `右侧面板${operation}专题`\n                    );\n                }\n\n                /**\n                 * 刷新地图专题\n                 * @param data - 当前高亮的专题数据\n                 */\n                function refreshMapSubject(lastSubject, newAppSubject) {\n                    // 发送socket消息\n                    sendSetAppARMsg(lastSubject, 'remove');\n                    // 发送socket消息\n                    sendSetAppARMsg(newAppSubject, 'add');\n                }\n\n                /**\n                 * 点击菜单项\n                 * @param data - 当前菜单项的数据\n                 * @param node - 当前菜单项节点\n                 */\n                function clickNode(data, node) {\n                    // console.log(data,node)\n                    /* 点击目录  或 点击的是当前选中项 不触发选中事件 */\n                    if (!node.isLeaf || state.currentMenuItem.RES_ID === data.RES_ID) {\n                        // 高亮当前选择节点的一级目录\n                        menuTreeRef.value.setCurrentKey(state.currentActiveFirstLevelMenuId);\n                        return;\n                    }\n                    // 设置一级节点高亮\n                    setFirstNodeActive(node);\n\n                    // 判断上一次选中的专题有没有选中复选框\n                    if (state.checkedIds.includes(state.currentMenuItem.RES_ID)) {\n                        // 有选中复选框----------------------\n                        // 不做处理\n                    } else if (!state.checkedIds.includes(state.currentMenuItem.RES_ID)) {\n                        // 没有选中复选框---------------------------\n                        // 判断点击的专题是否有页面\n                        if (data.MENU_URL) {\n                            // 发送socket消息：更新右侧菜单数据\n                            sendShowRightViewMsg(state.currentMenuItem, 'remove');\n                        }\n                        // 发送socket消息: 地图删除专题\n                        sendSetAppARMsg(state.currentMenuItem, 'remove');\n                    }\n\n                    // 更新高亮项\n                    state.currentMenuItem = data;\n                    // 判断点击的专题是否有页面\n                    if (data.MENU_URL) {\n                        // 发送socket消息：更新右侧菜单数据\n                        sendShowRightViewMsg(data, 'highlight');\n                    }\n\n                    // 判断最新选中的专题有没有选中复选框\n                    if (state.checkedIds.includes(data.RES_ID)) {\n                        // 有选中复选框----------------------\n                        /*\n                          地图逻辑：\n                          1.专题：先清掉所有专题，再重新添加，高亮的在最后；\n                          2.专题图层：不处理\n                        */\n                        // 目录的扩展字段\n                        // const extendField = data.EXT1 ? JSON.parse(data.EXT1) : {};\n                        // console.log(extendField.type, \"extendField.type\");\n                        // type 字段不存在则代表是专题\n                        // if (!extendField.type) {\n                        // console.log('选中的是专题');\n\n                        // 过滤一下checkedData数组，清除图层的时候，数组中不包含当前选中的专题\n                        let filteredCheckedData = []\n                        state.checkedData.forEach((item) => {\n                            if (item.RES_ID !== data.RES_ID) {\n                                filteredCheckedData.push(item);\n                            }\n\n                        });\n\n                        // 发送socket消息: 地图删除专题\n                        sendSetAppARMsg(state.checkedData, 'remove');\n                        // 发送socket消息: 地图添加专题\n                        sendSetAppARMsg([...filteredCheckedData, data], 'add');\n                        // } else if (extendField.type && extendField.type === \"layer\") {\n                        //   //type 字段存在并且等于layer则代表是图层\n                        //   console.log('选中的是图层');\n                        // }\n                    } else if (!state.checkedIds.includes(data.RES_ID)) {\n                        // 没有选中复选框---------------------------\n                        // 发送socket消息: 地图添加专题\n                        sendSetAppARMsg(data, 'add');\n                        // 判断点击的专题是否有页面\n                        if (data.MENU_URL) {\n                            // 发送socket消息：更新右侧菜单数据\n                            sendShowRightViewMsg(data, 'add');\n                        }\n                    }\n                    console.log(state.checkedData, 'state.checkedData');\n                }\n\n                let timeoutId;\n\n                /**\n                 * 点击菜单项\n                 * @param data {array} - 当前菜单项的数据\n                 * @param node {object} - 当前菜单项节点\n                 */\n                function handleClickNode(data, node) {\n                    console.log(data, node, '点击tree');\n                    localStorage.removeItem('isShowSubjectSelect');\n                    localStorage.setItem('isShowSubjectSelect', data.RES_ID);\n                    if (!data[propsMap.children].length) {\n                        if (timeoutId) {\n                            clearTimeout(timeoutId);\n                        }\n                        timeoutId = setTimeout(() => {\n                            clickNode(data, node);\n                            timeoutId = undefined;\n                        }, 100);\n                    } else {\n                        // 高亮当前选择节点的一级目录\n                        menuTreeRef.value.setCurrentKey(state.currentActiveFirstLevelMenuId);\n                    }\n                }\n\n                /**\n                 * 触发复选（专题）\n                 * @param {*} data - 传递给 data 属性的数组中该节点所对应的对象\n                 * @param {*} node - 节点\n                 */\n                function handleCheck(data, node) {\n\n                    // 判断是勾选还是取消\n                    if (!node.checked) {\n                        // 取消勾选--------------------\n                        // 逻辑：取消勾选高亮的专题不做处理\n                        if (data.RES_ID !== state.currentMenuItem.RES_ID) {\n                            // 判断点击的专题是否有页面\n                            if (data.MENU_URL) {\n                                // 发送socket消息：添加右侧菜单数据\n                                sendShowRightViewMsg(data, 'remove');\n                            }\n                            // 发送socket消息: 地图删除专题\n                            sendSetAppARMsg(data, 'remove');\n                        }\n                        // 删除点击的专题的数据\n                        const i = state.checkedData.findIndex((item) => item.RES_ID === data.RES_ID);\n                        state.checkedData.splice(i, 1);\n                    } else {\n\n                        // 勾选--------------------------\n                        // 逻辑：勾选高亮的专题不做处理\n                        if (data.RES_ID !== state.currentMenuItem.RES_ID && !state.checkedIds.includes(state.currentMenuItem.RES_ID)) {\n                            // 判断点击的专题是否有页面\n                            if (data.MENU_URL) {\n                                // 发送socket消息：添加右侧菜单数据\n                                sendShowRightViewMsg(state.currentMenuItem, 'remove');\n                            }\n                        }\n                        if (data.RES_ID !== state.currentMenuItem.RES_ID) {\n                            // 高亮当前选择节点的一级目录\n                            menuTreeRef.value.setCurrentKey(state.currentActiveFirstLevelMenuId);\n                            // 设置一级节点高亮\n                            setFirstNodeActive(node);\n\n                            // 判断点击的专题是否有页面\n                            if (data.MENU_URL) {\n                                // 发送socket消息：添加右侧菜单数据\n                                sendShowRightViewMsg(data, 'add');\n                                // 发送socket消息：更新右侧菜单数据\n                                sendShowRightViewMsg(data, 'highlight');\n                            }\n\n                            /*\n                              逻辑：\n                              1.勾选专题：先清掉所有专题，再重新添加，高亮的在最后；\n                              2.勾选专题图层：只做添加操作\n                            */\n\n                            // 目录的扩展字段\n                            // const extendField = data.EXT1 ? JSON.parse(data.EXT1) : {};\n                            // console.log(extendField.type, \"extendField.type\");\n                            // type 字段不存在则代表是专题\n                            // if (!extendField.type) {\n                            // console.log('勾选的是专题');\n\n\n                            /*若勾选项里没有包含高亮菜单*/\n                            if (!state.checkedIds.includes(state.currentMenuItem.RES_ID)) {\n                                // 过滤一下checkedData数组，清除图层的时候，数组中不包含当前选中的专题\n                                let filteredCheckedData = []\n                                state.checkedData.forEach((item) => {\n                                    if (item.RES_ID !== state.currentMenuItem.RES_ID) {\n                                        filteredCheckedData.push(item);\n                                    }\n\n                                });\n                                // 发送socket消息: 地图删除专题\n                                sendSetAppARMsg([...filteredCheckedData, state.currentMenuItem], 'remove');\n                                // 发送socket消息: 地图添加专题\n                                sendSetAppARMsg([...filteredCheckedData, data], 'add');\n                            } else {\n                                // 发送socket消息: 地图删除专题\n                                sendSetAppARMsg([...state.checkedData], 'remove');\n                                // 发送socket消息: 地图添加专题\n                                sendSetAppARMsg([...state.checkedData, data], 'add');\n                            }\n                            // } else if (extendField.type && extendField.type === \"layer\") {\n                            //   //type 字段存在并且等于layer则代表是图层\n                            //   console.log('勾选的是图层');\n                            //   // 发送socket消息: 地图添加专题图层\n                            //   sendSetAppARMsg(data, \"add\");\n                            // }\n                        }\n                        // 更新高亮项\n                        state.currentMenuItem = data;\n                        // 记录点击的专题及专题图层\n                        state.checkedData.push(data);\n                    }\n\n                    // console.log(state.checkedData, \"state.checkedData\");\n                    // 更新当前勾选上的菜单项的id列表\n                    state.checkedIds = state.checkedData.map((item) => item.RES_ID);\n                }\n\n\n                /**\n                 * 触发复选（图层专题）\n                 * @param {*} data - 传递给 data 属性的数组中该节点所对应的对象\n                 * @param {*} node - 节点\n                 */\n                function handleLayerMenuCheck(data, node) {\n                    // 目录\n                    if (data[propsMap.children].length) {\n                        // 高亮当前选择节点的一级目录\n                        layerMenuTreeRef.value.setCurrentKey(state.currentActiveFirstLevelLayerMenuId);\n                    } else {\n                        // 判断是勾选还是取消\n                        if (!node.checked) {\n                            // 取消勾选--------------------\n\n                            // 取消选中项\n                            if (state.currentLayerMenuItem?.RES_ID === data.RES_ID) {\n                                state.currentLayerMenuItem = {};\n\n                                // 发送socket消息：控制右侧面板高亮专题\n                                sendShowRightViewMsg(state.currentMenuItem, 'highlight', 'layer');\n                            }\n\n                            // 逻辑：取消勾选的图层专题\n\n                            // 判断点击的图层专题是否有页面\n                            if (data.MENU_URL) {\n                                // 发送socket消息：添加右侧菜单数据\n                                sendShowRightViewMsg(data, 'remove', 'layer');\n                            }\n                            // 发送socket消息: 地图删除专题\n                            sendSetAppARMsg(data, 'remove');\n\n                            // 删除专题图层的数据\n                            const i = state.checkedData.findIndex((item) => item.RES_ID === data.RES_ID);\n                            state.checkedData.splice(i, 1);\n\n                            // 获取树选中的图层数量，设置一级目录是否要取消高亮\n                            const getCheckedNodes = layerMenuTreeRef.value.getCheckedNodes();\n                            if (!getCheckedNodes.length) {\n                                // 取消高亮当前选择节点的一级目录\n                                layerMenuTreeRef.value.setCurrentKey(null);\n                                state.currentActiveFirstLevelLayerMenuId = null;\n                                state.layerMenuTreeKey += 1;\n                            }\n\n                        } else {\n                            // 勾选--------------------------\n\n                            // 判断点击的图层专题是否有页面\n                            if (data.MENU_URL) {\n                                // 发送socket消息：添加右侧菜单数据\n                                sendShowRightViewMsg(data, 'add', 'layer');\n                                // 发送socket消息：更新右侧菜单数据\n                                sendShowRightViewMsg(data, 'highlight', 'layer');\n                            }\n                            // 发送socket消息: 地图添加专题图层\n                            sendSetAppARMsg(data, 'add');\n\n                            // 更新高亮项\n                            state.currentLayerMenuItem = data;\n                            // 记录点击的专题图层\n                            state.checkedData.push(data);\n\n                            if (!state.currentActiveFirstLevelLayerMenuId) {\n                                // 设置一级节点高亮\n                                setFirstNodeActive(node, 'layer');\n                            }\n                        }\n                    }\n                }\n\n                /**\n                 * 移除地图图层\n                 */\n                function removeMapLayer() {\n                    // 地图需要移除的图层数据\n                    const removeLayerData = state.checkedData;\n                    /* 若勾选项中没有当前选中项 并且 有选中项 */\n                    if (\n                        !state.checkedData.find(\n                            (item) => item.RES_ID === state.currentMenuItem.RES_ID\n                        )\n                        && state.currentMenuItem.RES_ID\n                    ) {\n                        // 当前选中项对应的专题也要移除\n                        removeLayerData.push(state.currentMenuItem);\n                    }\n                    if (removeLayerData.length) {\n                        // 发送socket消息: 地图删除专题\n                        sendSetAppARMsg(removeLayerData, 'remove');\n                    }\n                }\n\n                /**\n                 * 初始化数据\n                 */\n                function initData() {\n                    /* 若有数据 */\n                    if (state.menuData.length && state.menuData[0]?.[propsMap.children].length) {\n                        // 第一条专题数据\n                        const firstData = state.menuData[0][propsMap.children][0];\n                        // 重置勾选ID\n                        state.checkedIds = [];\n                        // 重置勾选菜单数据\n                        state.checkedData = [];\n                        // 初始化当前选中的菜单项数据\n                        state.currentMenuItem = {};\n                        state.currentLayerMenuItem = {};\n                        // 初始化当前高亮的一级菜单项id\n                        // state.currentActiveFirstLevelMenuId = state.menuData[0].RES_ID;\n                        state.currentActiveFirstLevelMenuId = '';\n\n                        setTimeout(() => {\n                            if (menuTreeRef.value) {\n                                // 高亮当前菜单\n                                // menuTreeRef.value.setCurrentKey(\n                                //   state.menuData[0][propsMap.children][0].RES_ID\n                                // );\n                                // // 一级菜单项节点\n                                // const firstLevelNode = menuTreeRef.value.getNode(\n                                //   state.menuData[0].RES_ID\n                                // );\n\n                                // if (firstLevelNode) {\n                                //   // 高亮一级菜单项\n                                //   firstLevelNode.isCurrent = true;\n                                // }\n\n                                /* 选中专题 */\n                                const node = menuTreeRef.value.getNode(firstData.RES_ID);\n                                if (node) {\n                                    clickNode(firstData, node);\n                                }\n                            }\n                        });\n                    }\n                }\n\n                /**\n                 * 复选框是否可见\n                 * @param node - 当前节点\n                 * @param data - 当前节点数据\n                 */\n                function checkboxVisible(node, data) {\n                    /* 目录不显示复选框 */\n                    if (!node.isLeaf) {\n                        return false;\n                    }\n\n                    try {\n                        // 目录的扩展字段\n                        const extendField = data?.EXT1 ? JSON.parse(data.EXT1) : {};\n\n                        if (extendField.type && extendField.type === 'layer') {\n                            return true;\n                        }\n                    } catch (error) {\n                        console.error(error, data?.EXT1, 'EXT1扩展字段数据格式不对');\n                    }\n                    return state.isShowSubjectCheckbox;\n                }\n\n                /**\n                 * 扁平化树形数据\n                 * @param data - 树形数据\n                 */\n                function treeToArray(data) {\n                    const arr = [];\n                    const expanded = (datas) => {\n                        if (datas && datas.length > 0) {\n                            datas.forEach((item) => {\n                                arr.push(item);\n                                if (item.CHILDREN) {\n                                    expanded(item.CHILDREN);\n                                    // eslint-disable-next-line no-param-reassign\n                                    delete item.CHILDREN;\n                                }\n                            });\n                        }\n                    };\n                    expanded(data);\n                    return arr;\n                }\n\n                /**\n                 * 更新图层勾选状态\n                 * @param layerStatusList - 图层状态列表\n                 * @param layerList - 图层列表\n                 */\n                function updateLayerCheck(layerStatusList, layerList) {\n                    // console.log(layerStatusList, layerList);\n                    layerStatusList.forEach((item) => {\n                        if (item.visible) {\n                            // 若图层可见，则查找图层的id在图层列表数据里有没有，有再添加\n                            const findIndex = layerList.findIndex((val) => val.RES_NAME === item.layerId);\n                            if (findIndex > -1) {\n                                // 更新当前勾选的图层ID\n                                state.checkedIds.push(layerList[findIndex].RES_ID);\n                                // 更新当前勾选上的菜单项的数据列表\n                                state.checkedData.push(layerList[findIndex]);\n                            }\n                        } else {\n                            // 若图层不可见，则从当前勾选的图层ID列表中移除\n                            const findIndex = state.checkedData.findIndex((val) => val.RES_NAME === item.layerId);\n                            if (findIndex > -1) {\n                                // 更新当前勾选的图层ID\n                                state.checkedIds.splice(findIndex, 1);\n                                // 更新当前勾选上的菜单项的数据列表\n                                state.checkedData.splice(findIndex, 1);\n                            }\n                        }\n                    })\n\n                    // 所有的勾选的图层ID\n                    const allCheckedData = state.checkedData.map((val) => val.RES_ID);\n                    // 设置勾选状态\n                    layerMenuTreeRef.value.setCheckedKeys(allCheckedData, false);\n\n                    // 获取树选中的图层数量，设置一级目录是否要取消高亮\n                    const getCheckedNodes = layerMenuTreeRef.value.getCheckedNodes();\n                    if (getCheckedNodes.length) {\n                        // 设置一级节点高亮\n                        state.currentActiveFirstLevelLayerMenuId = state.layerMenuData[0].RES_ID;\n                        // 高亮当前选择节点的一级目录\n                        layerMenuTreeRef.value.setCurrentKey(state.currentActiveFirstLevelLayerMenuId);\n                    }\n\n                    // if(state.checkedData.length && !state.currentActiveFirstLevelLayerMenuId){\n                    //   // 设置一级节点高亮\n                    //   state.currentActiveFirstLevelLayerMenuId = state.layerMenuData[0].RES_ID;\n                    //   // 高亮当前选择节点的一级目录\n                    //   layerMenuTreeRef.value.setCurrentKey(state.currentActiveFirstLevelLayerMenuId);\n                    // }\n                }\n\n                onMounted(() => {\n                    // 默认发送socket消息，通知鹿小图加载完成\n                    rootData.rootSocket.emit('message', {\n                        room: global.socketRoom, // 房间号\n                        type: 'leftMenuMounted', // 消息名称\n                        data: {} // 发送的数据\n                    });\n\n                    // 默认发送socket消息，通知鹿小图加载完成\n                    // rootData.rootSocket.emit(\"message\", {\n                    //   room: global.socketRoom, // 房间号\n                    //   type: \"loadMenuData\", // 消息名称\n                    //   data: {\n                    //     // menuId:'58b82e7a-8b9f-4e6a-863e-4f8292885bf3',// 佛山\n                    //     menuId:'b5b45013-49d8-43e8-98a6-6179431c99de', // 鹿小图\n                    //   }, // 发送的数据\n                    // });\n\n                    // 接收socket消息\n                    rootData.rootOn('rootSocket:change', (data) => {\n                        // console.log(data, \"专题菜单接收到消息---\");\n\n                        /* 加载菜单数据 */\n                        if (data.type === 'loadMenuData') {\n                            console.log(data, '专题菜单接收到切换数据的消息---');\n\n                            // 移除地图图层\n                            removeMapLayer();\n\n                            try {\n                                const activeMenuItem = data.data.menuId;\n                                // 所有的菜单数据-字符串\n                                const allMenuDataStr = localStorage.getItem('lxtAsideMenusMap');\n                                // 所有的菜单数据\n                                let allMenuData = allMenuDataStr ? JSON.parse(allMenuDataStr) : [];\n\n                                console.log(allMenuData, 'allMenuData');\n\n                                if (allMenuData && activeMenuItem) {\n                                    // 更新菜单数据（所有的数据，不区分是专题还是专题图层）\n                                    state.data = allMenuData[activeMenuItem] || [];\n\n                                    // 更新专题和专题图层的菜单数据\n                                    state.menuData = [];\n                                    state.layerMenuData = [];\n                                    state.data.forEach((item) => {\n                                        if (item.EXT1) {\n                                            const typeExt1Obj = JSON.parse(item.EXT1);\n                                            // 图层专题\n                                            if (typeExt1Obj.type === 'layer') {\n                                                state.layerMenuData.push(item);\n                                            } else {\n                                                // 专题\n                                                state.menuData.push(item);\n                                            }\n                                        } else {\n                                            // 专题\n                                            state.menuData.push(item);\n                                        }\n                                    });\n                                    console.log(state.menuData, '专题数据')\n                                    state.showMenuTree = checkShowMenuTree();\n                                    // 初始化数据-默认选中第一个专题\n                                    initData();\n\n                                    // if (state.currentMenuItem) {\n                                    //   // 发送socket消息: 地图添加专题\n                                    //   sendSetAppARMsg(state.currentMenuItem, \"add\");\n                                    //   // 判断默认选中的专题是否有页面\n                                    //   if(state.currentMenuItem.MENU_URL){\n                                    //     // 发送socket消息：添加右侧菜单数据\n                                    //     sendShowRightViewMsg(state.currentMenuItem, \"add\");\n                                    //     // 发送socket消息：控制右侧面板高亮专题\n                                    //     sendShowRightViewMsg(state.currentMenuItem, \"highlight\");\n                                    //   }\n                                    // }\n                                }\n                                // console.log('刷新树接收的数据 ---', data);\n                            } catch (error) {\n                                return '参数格式错误';\n                            }\n                        } else if (data.type === 'activeSpecial') {\n                            console.log(data, '专题菜单接收到切换tabs的消息---');\n                            if (data.data.type === 'layer') {\n                                // 发送socket消息：控制右侧面板高亮专题\n                                sendShowRightViewMsg(data.data.rawData, 'highlight');\n                                // 清空图层专题的高亮\n                                state.currentLayerMenuItem = data.data.rawData;\n                            } else {\n                                if (data.data.id === state.currentMenuItem.RES_ID) {\n                                    // 发送socket消息：控制右侧面板高亮专题\n                                    sendShowRightViewMsg(state.currentMenuItem, 'highlight');\n                                    // 清空图层专题的高亮\n                                    // state.currentLayerMenuItem = {};\n                                } else {\n                                    /* 监听专题切换 */\n                                    const node = menuTreeRef.value.getNode(data.data.id);\n                                    if (node) {\n                                        clickNode(data.data.rawData, node);\n                                    }\n                                }\n                            }\n                            // if(data.data.id === state.currentMenuItem.RES_ID){\n                            //   // 发送socket消息：控制右侧面板高亮专题\n                            //   sendShowRightViewMsg(state.currentMenuItem, \"highlight\");\n                            //   // state.currentLayerMenuItem = {};\n                            // } else if(data.data.rawData?.menuType === 'layer'){\n                            //   // 发送socket消息：控制右侧面板高亮专题\n                            //   sendShowRightViewMsg(state.currentLayerMenuItem, \"highlight\");\n                            // } else {\n                            //   /* 监听专题切换 */\n                            //   const node = menuTreeRef.value.getNode(data.data.id);\n                            //   if (node) {\n                            //     clickNode(data.data.rawData, node);\n                            //   }\n                            // }\n                        } else if (data.type === 'resetActiveMenu') {\n                            /* 监听菜单高亮重置 */\n\n                            /* 非当前模块发的消息才重置高亮 */\n                            if (data.data !== 'subject') {\n                                /* 若有选中的图层数据 且 没有勾选上当前菜单项 */\n                                if (\n                                    Object.keys(state.currentMenuItem).length &&\n                                    !state.checkedIds.includes(state.currentMenuItem.RES_ID)\n                                ) {\n                                    // 发送socket消息:右侧面板移除当前选中项\n                                    sendShowRightViewMsg(state.currentMenuItem, 'remove');\n                                }\n                                state.currentMenuItem = {};\n                            }\n\n                        } else if (data.type === 'getMenuLayerStatus') {\n                            /* 监听获取专题或图层状态消息 */\n                            const checkedData = state.checkedData.map((val) => ({ ...val, isChecked: true }));\n                            // 所有高亮+勾选的菜单项\n                            const allActiveMenu = state.currentMenuItem && state.checkedIds.includes(state.currentMenuItem.RES_ID) ? checkedData : [...checkedData, {\n                                ...state.currentMenuItem,\n                                isChecked: false\n                            }];\n                            // 所有高亮的专题\n                            const allAppActiveMenu = [];\n                            // 所有高亮的图层\n                            const allLayerActiveMenu = [];\n                            allActiveMenu.forEach((item) => {\n                                try {\n                                    // 目录的扩展字段\n                                    const extendField = item.EXT1 ? JSON.parse(item.EXT1) : {};\n\n                                    // type 字段不存在则代表是专题\n                                    if (!extendField.type) {\n                                        allAppActiveMenu.push(item);\n                                    } else {\n                                        allLayerActiveMenu.push(item);\n                                    }\n                                } catch (err) {\n                                    console.error(err, `${item.EXT1}解析错误`)\n                                }\n                            });\n\n                            const menu = allAppActiveMenu.map((item) => {\n                                // 目录的扩展字段\n                                const extendField = item.EXT1 ? JSON.parse(item.EXT1) : {};\n                                return {\n                                    resCode: extendField?.resCode || '',\n                                    opType: 'add',\n                                    resName: item.RES_NAME,\n                                    appURL: extendField?.appURL || '',\n                                    highlight: item.RES_ID === state.currentMenuItem.RES_ID,\n                                    isChecked: item.isChecked\n                                }\n                            });\n\n                            const menuLayer = allLayerActiveMenu.map((item) => {\n                                // 目录的扩展字段\n                                const extendField = item.EXT1 ? JSON.parse(item.EXT1) : {};\n                                return {\n                                    libraryId: extendField.resCode,\n                                    layerId: extendField.resCode,\n                                    opType: 'add',\n                                    highlight: item.RES_ID === state.currentMenuItem.RES_ID,\n                                    isChecked: item.isChecked\n                                }\n                            });\n\n                            // 发送socket消息: 地图添加/删除专题\n                            rootData.rootSocket.emit('message', {\n                                room: global.socketRoom, // 房间号\n                                type: 'sendMenuStatus', // 消息名称\n                                data: {\n                                    menu,\n                                    menuLayer\n                                }\n                            });\n\n                            console.log('专题菜单发送sendMenuStatus消息---', {\n                                menu,\n                                menuLayer\n                            })\n                        } else if (data.type === 'layerStatus') {\n                            console.log(data, '专题图层菜单接收到layerStatus消息---');\n                            if (!Array.isArray(data.data)) {\n                                return;\n                            }\n\n                            // 扁平化的图层数据\n                            const flatArr = treeToArray(JSON.parse(JSON.stringify(state.layerMenuData)));\n                            // 更新图层勾选状态\n                            updateLayerCheck(data.data, flatArr);\n                        }\n                    });\n\n                    // setTimeout(()=>{\n                    //   // 扁平化的图层数据\n                    //   const flatArr = treeToArray(JSON.parse(JSON.stringify(state.layerMenuData)));\n                    //   console.log(flatArr);\n                    //   updateLayerCheck([{layerId: '区县', visible: true}], flatArr);\n                    // },5000)\n                });\n\n                onUnmounted(() => {\n                    rootData.rootOff('rootSocket:change');\n                });\n\n                    // 筛选面板相关方法\n                const handleSearch = () => {\n                    // 这里可以添加搜索逻辑\n                    console.log('搜索关键词:', state.searchKeyword);\n                };\n\n                const clickTag = (tag) => {\n                    const index = state.selectedTags.indexOf(tag);\n                    if (index === -1) {\n                        state.selectedTags.push(tag);\n                    } else {\n                        state.selectedTags.splice(index, 1);\n                    }\n                };\n\n                return {\n                    ...toRefs(state),\n                    themeName,\n                    propsMap,\n                    menuTreeRef,\n                    layerMenuTreeRef,\n                    handleClickNode,\n                    checkShowMenuTree,\n                    handleCheck,\n                    checkboxVisible,\n                    handleLayerMenuCheck,\n                    handleSearch,\n                    clickTag\n                };\n            }\n        };\n\n        /**\n         * 这里入口组件内容\n         */\n        const MainComponent = {\n            template:\n                // 默认页面 ———— 一直显示 页面中的组件根据显示条件显示\n                // '<router-view></router-view>',\n                '<subject-component v-if=\\'state.isSocketConnect\\'></subject-component>',\n            // '<layer-component></layer-component>',\n            components: {\n                subjectComponent\n                // layerComponent,\n            },\n            setup() {\n                // 获取缓存里的鹿小图全局变量数据\n                const lxtGlobal = localStorage.getItem('lxtGlobal')\n                    ? JSON.parse(localStorage.getItem('lxtGlobal'))\n                    : {};\n\n                const rootEmitter = mitt();\n\n                let global = reactive(\n                    /* 插入应用级别的全局变量 相当于store中的rootData */\n                    /* globalVariableStart */\n                    {\n                        socketIp:\n                            lxtGlobal.socketIp || 'http://192.168.0.202:7777/socketPath',\n                        socketPath: lxtGlobal.socketPath || '/socketPath',\n                        socketRoom: lxtGlobal.socketRoom || roomId || 'admin'\n                    }\n                    /* globalVariableEnd */\n                );\n\n                let state = reactive({\n                    isSocketConnect: false // socket 是否连接\n                });\n\n                // 创建 socket 连接\n                let socket = null;\n                if (global.socketIp) {\n                    const options = {};\n                    if (global?.socketPath) {\n                        options.path = global.socketPath;\n                    }\n                    socket = io(global.socketIp, options);\n                }\n\n                provide('root', {\n                    ...toRefs(global),\n                    rootEmit: rootEmitter.emit, // mitt 发送\n                    rootOn: rootEmitter.on, // mitt 接收\n                    rootOff: rootEmitter.off, // mitt 移除\n                    rootSocket: socket // socket 实例\n                });\n\n                /**\n                 * 监听修改全局变量事件\n                 * @param obj 修改后的变量\n                 */\n                rootEmitter.on('rootData:change', (obj) => {\n                    global = _.set(global, obj.key, obj.value);\n\n                    if (global.socketIp && global.socketRoom) {\n                        // 全局变量发生变化时，需要通知其他屏，这里直接发送socket消息\n                        socket.emit('message', {\n                            room: global.socketRoom, // 房间号\n                            type: 'internalType:ChangeGlobalValue', // 需要保证此类型唯一性\n                            data: obj\n                        });\n                    }\n                });\n\n                /**\n                 * 连接成功时进入对应的房间\n                 */\n                if (socket) {\n                    socket.on('connect', function () {\n                        if (global.socketIp && global.socketRoom) {\n                            // 进入房间\n                            socket.emit('join-room', global.socketRoom);\n                            state.isSocketConnect = true;\n                        }\n                    });\n                }\n\n                /**\n                 * 接收到 socket 消息时触发\n                 * @param data 消息内容 {type:'消息类型',data:实际的数据,任何类型}\n                 */\n                if (socket) {\n                    socket.on('message', function (data) {\n                        // socket 监听全局变量的变化，然后合并值\n                        if (data.type === 'internalType:ChangeGlobalValue') {\n                            // 合并 globa 的值\n                            global = _.set(global, data.data.key, data.data.value);\n                        } else {\n                            // 发送消息，通知其他页面\n                            rootEmitter.emit('rootSocket:change', data);\n                        }\n                    });\n                }\n\n                // 监听 socket 房间的变化\n                watch(\n                    () => global.socketRoom,\n                    (val, oldVal) => {\n                        if (socket) {\n                            // 离开房间\n                            socket.emit('leave-room', oldVal);\n                            // 进入房间\n                            socket.emit('join-room', val);\n                        }\n                    }\n                );\n\n                onBeforeUnmount(() => {\n                    // 离开房间\n                    socket.emit('leave-room', global.socketRoom);\n                    // 断开连接\n                    socket.disconnect();\n                });\n\n                return {\n                    state\n                };\n            }\n        };\n\n        const app = createApp(MainComponent);\n        /* 下面的操作是去掉控制台Vue输出的警告信息  如果想开启警告信息可以把这个空函数注释掉 */\n        app.config.warnHandler = function (msg, vm, trace) {\n            // 空函数，不做任何处理\n        };\n        app\n            .use(ElementPlus, {\n                size: 'small'\n            })\n            .mount('#app');\n    </script>\n</body>\n\n</html>",
scrolling:"no",
cStyle:{"wrapper":{"default":{"width":"1921px","height":"1074px","position":"unset","left":"-4px","top":"4px"}}},
socketUrl:"http://192.168.0.202:7777/socketPath",
roomId:"admin", 
 },
},
});let apiRegistry = {}; 

 function handleWindowResize() {
                            setPageScale('t-l-c-test', global.appScaleMode, 'normal');
                           
                            // 获取窗口的宽度和高度
                            var windowWidth = window.innerWidth;

}
onMounted(() => {
setPageScale('t-l-c-test', global.appScaleMode, 'normal');
window.addEventListener('resize', handleWindowResize);
handleWindowResize();
/* 发送socket消息 */
rootData.rootSocket.emit('message', {
room: global.socketRoom, // 房间号
type: "showLegoDialog", // 消息名称
data: {"dialogName":"airOverviewSiteForHourDayComponent"}, // 发送的数据
});
});
// 页面离开时的操作
onUnmounted(() => {
// 移除监听事件
window.removeEventListener('resize', handleWindowResize);
});
 const tIframex0x0ComputedData = computed(() => _.merge({}, componentState.tIframex0x0.default, componentState.tIframex0x0?.[state.screenSize]));

      if (apiRegistry && Object.keys(apiRegistry).length) {
        // 加载初始接口
        loadInitialApis(apiRegistry, apiDataCache, componentState, componentPropBindingMap, function(cacheKey, data, apiName) {
            // 更新缓存
            apiDataCache[cacheKey] = data;
            apiDataMap[apiName] = data;
        });

        /* 监听组件可视变化，获取组件绑定的接口数据 */
        watchComponentVisible(apiRegistry, apiDataCache, componentState, componentPropBindingMap, function(cacheKey, data, apiName) {
            // 更新缓存
            apiDataCache[cacheKey] = data;
            apiDataMap[apiName] = data;
        });
      }
       
       /* 获取接口数据 */
       const getApiData = (id, key) => {
          return computed(() => getDataValue(apiDataMap, componentPropBindingMap[id][key]));
       };
      
return {
replaceCssVariables,
getApiData,
filterData,
componentPropBindingMap,
global,
state,
componentState,
tIframex0x0ComputedData,
};
},
};