# 2026-06-09 工作日志

## micro-app-template API 文档编写

- 基于对 `micro-app-template` 项目全部源码的深入分析，编写了完整的开发者 API 文档
- 文档路径：`micro-app-template/API.md`
- 覆盖内容：
  - 三通道通信架构（data/setData、dispatch、globalData）
  - 微应用侧 API（useHostBridge、callHost、sendToHost、useGlobalData、useGlobalDataKey、setGlobalData、TMap 代理、弹窗穿透、inject('root')、过滤器、独立运行降级）
  - 基座侧 API（useChildBridge、useGlobalData、bindReactiveToGlobal、弹窗组件注册）
  - 全局变量参考（服务地址、行政区查询、Socket 配置、字体大小等）
  - 全局函数参考（DOM 操作、时间日期、区域环境、数据处理、图表、URL 参数等）
  - 工具函数参考（mountDynamicComponent、ResourceLoader、ComponentLoader、接口请求框架、数据过滤等）
  - WebSocket API、生命周期与挂载、环境变量、快速参考卡

## 命名审查与多厂商协作方案编写

- 对 micro-app-template 通信交互层全部 API 命名进行了专业度审查
- 识别出三个核心问题：(1) 与 micro-app 框架 API 同名但语义不同（setData/setGlobalData）(2) 基座/子应用双侧命名不对称（useChildBridge vs useHostBridge、dataToChild vs hostData）(3) data 太泛无法区分
- 提出了更专业的命名方案：useHostChannel/useMicroChannel、outboundProps/inboundProps、patchProps、sharedState/patchSharedState 等
- 编写了《多厂商协作开发方案》汇报文档（面向领导/甲方），路径：`micro-app-template/多厂商协作开发方案.md`
- 文档覆盖：方案选型论证（4方案对比）、整体架构（三通道模型）、开发流程（4阶段）、核心能力（全局数据/RPC/地图代理/弹窗穿透）、安全保障、厂商开发规范、集成上线检查清单

## PPT 制作

- 将《多厂商协作开发方案》转换为 PPT 格式，路径：`micro-app-template/多厂商协作开发方案.pptx`
- 使用 PptxGenJS 从零构建，14 页幻灯片，深蓝+薄荷绿配色方案
- 幻灯片：封面→背景与挑战→建设目标→方案选型对比→整体架构→三通道通信→开发流程→核心能力(数据+RPC)→核心能力(地图+弹窗)→安全保障→厂商规范→集成上线→优势总结→结尾

## 命名方案待落地

- 通信层 API 命名优化方案已提出但未改代码，核心5项：
  1. useChildBridge/useHostBridge → useHostChannel/useMicroChannel
  2. dataToChild/hostData → outboundProps/inboundProps
  3. setGlobalData → patchSharedState（与框架 API 同名但语义不同）
  4. useGlobalDataKey → useSharedSelector
  5. provide('root') → provide(SHELL_KEY) 用 Symbol
